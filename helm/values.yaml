# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  repository: StasSweepy
  # Overrides the image tag whose default is the chart appVersion.
  tag: "v1.0.7-e8e9e0b"
  arch: amd64
  os: linux
secret:
  name: "testbot1"
  env: "token"
  key: "TELE_TOKEN"
